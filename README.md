<div align="center">

# 🎯 PromiseNow

**약속부터 만남까지 한 화면에서 함께하는 즐거움**

[![Spring Boot](https://img.shields.io/badge/Spring%20Boot-3.5.3-brightgreen.svg)](https://spring.io/projects/spring-boot)
[![React](https://img.shields.io/badge/React-19.1.0-blue.svg)](https://react.dev/)
[![TypeScript](https://img.shields.io/badge/TypeScript-5.8.3-blue.svg)](https://www.typescriptlang.org/)
[![Java](https://img.shields.io/badge/Java-21-orange.svg)](https://www.oracle.com/java/)
[![License](https://img.shields.io/badge/License-SSAFY%20Project-yellow.svg)](LICENSE)

[프로젝트 소개](#-프로젝트-소개) • [주요 기능](#-주요-기능) • [빠른 시작](#-빠른-시작) • [기술 스택](#️-기술-스택) • [문서](#-문서)

</div>

---

## 📖 프로젝트 소개

**PromiseNow**는 친구들과의 약속을 더 쉽고 재미있게 만들어주는 실시간 약속 관리 서비스입니다. 

약속 방 생성부터 일정 조율, 실시간 위치 공유, 화상 통화까지 모든 기능을 한 곳에서 제공하여 사용자들이 더 효율적으로 만날 수 있도록 돕습니다.

### ✨ 핵심 가치

- **실시간 협업**: WebSocket과 WebRTC를 활용한 실시간 통신
- **직관적인 UX**: 모바일 친화적인 반응형 디자인
- **안전한 인증**: OAuth2 + JWT 기반 보안 인증
- **확장 가능한 아키텍처**: 마이크로서비스 지향 설계

---

## 🎨 주요 기능

### 🏠 방 관리
- 약속 방 생성 및 초대 코드 공유
- 참여자 관리 및 권한 설정
- 방 상태 관리 (대기/진행/완료/취소)

### 📅 일정 조율
- When2Meet 스타일의 캘린더 기반 일정 조율
- 실시간 가능 시간 표시
- 자동 일정 추천 기능

### 💬 실시간 채팅
- WebSocket 기반 실시간 메시지 전송
- 이미지 업로드 및 공유
- PINO(핀 메시지) 기능

### 📍 위치 공유
- 실시간 위치 추적 및 공유
- 약속 장소까지의 거리 계산
- 도착 순위 리더보드

### 🎲 룰렛
- 만날 장소 랜덤 선택
- 사용자별 룰렛 항목 등록 및 관리

### 📹 화상 통화
- WebRTC 기반 고품질 화상 통화
- 다중 사용자 동시 참여
- 오디오/비디오 제어

### 🏆 도착 순위
- 실시간 도착 순위 표시
- WebSocket 기반 실시간 업데이트

---

## 🏗️ 시스템 아키텍처

```
┌─────────────────────────────────────────────────────────────┐
│                        Frontend (React)                      │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐  │
│  │  Pages   │  │ Features │  │  Hooks   │  │  Stores  │  │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘  │
└─────────────────────────────────────────────────────────────┘
                            │
                            │ HTTPS / WSS
                            │
        ┌───────────────────┼───────────────────┐
        │                   │                   │
┌───────▼────────┐  ┌───────▼────────┐  ┌───────▼────────┐
│  Spring Boot   │  │  WebRTC Server  │  │     Nginx      │
│   (Backend)    │  │   (mediasoup)   │  │  (Reverse      │
│                │  │                 │  │    Proxy)      │
│  ┌──────────┐  │  │  ┌──────────┐  │                 │
│  │   REST   │  │  │  │  WebRTC   │  │                 │
│  │   API    │  │  │  │ Signaling │  │                 │
│  └──────────┘  │  │  └──────────┘  │                 │
│  ┌──────────┐  │                   │                 │
│  │WebSocket │  │                   │                 │
│  │ (STOMP)  │  │                   │                 │
│  └──────────┘  │                   │                 │
└───────┬────────┘  └───────────────────┘                 │
        │                                                  │
        │                                                  │
┌───────▼────────┐                              ┌─────────▼────┐
│     MySQL      │                              │    Redis     │
│   (Database)   │                              │   (Cache)    │
└────────────────┘                              └───────────────┘
```

---

## 🚀 빠른 시작

### 📋 사전 요구사항

- **Docker & Docker Compose** (인프라 서비스용)
- **Java 21** (백엔드 개발용)
- **Node.js 20+** (프론트엔드 및 WebRTC 서버용)
- **pnpm** (프론트엔드 패키지 매니저)

### 🎯 전체 개발 환경 실행 (권장)

```bash
# 백엔드 + 프론트엔드 + 인프라 모두 자동 실행
./scripts/full-dev.sh
```

이 스크립트는 다음을 자동으로 수행합니다:
1. ✅ Docker 인프라 서비스 시작 (MySQL, Redis)
2. ✅ 데이터베이스 초기화
3. ✅ 백엔드 빌드 및 실행
4. ✅ 프론트엔드 의존성 설치 및 개발 서버 시작

### 📝 수동 실행 방법

#### 1. 환경 변수 설정

```bash
# env.example 파일을 참고하여 환경 변수 설정
cp env.example .env

# backend-spring/.env 파일 생성 (백엔드용)
# ⚠️ 실제 비밀값은 저장소에 커밋하지 마세요!
```

**필수 환경 변수:**
- `SPRING_DATASOURCE_PASSWORD`: MySQL 비밀번호
- `SPRING_JWT_TOKEN`: JWT 시크릿 키
- `SPRING_KAKAO_CLIENT_ID`: 카카오 OAuth Client ID
- `SPRING_KAKAO_CLIENT_SECRET`: 카카오 OAuth Client Secret
- `WEBHOOK_SLACK_URL`: Slack 웹훅 URL (선택사항)

#### 2. 인프라 서비스 시작

```bash
# MySQL, Redis 시작
docker-compose -f docker-compose.local.yml up -d mysql redis
```

#### 3. 백엔드 실행

```bash
cd backend-spring

# 테스트 파일 제거 (빌드 오류 방지)
rm -rf src/test

# 빌드 및 실행
./gradlew clean build -x test
./gradlew bootRun
```

#### 4. WebRTC 서버 실행 (선택사항)

```bash
cd backend-webrtc

# 의존성 설치
npm install

# 개발 서버 실행
npm start
```

#### 5. 프론트엔드 실행

```bash
cd frontend

# 의존성 설치
pnpm install

# 개발 서버 시작
pnpm run dev
```

### 🧹 개발 환경 정리

```bash
# 모든 서비스 종료 및 정리
./scripts/cleanup.sh
```

---

## 🌐 접속 정보

### 로컬 개발 환경
| 서비스 | URL | 설명 |
|--------|-----|------|
| 프론트엔드 | http://localhost:5173 | React 개발 서버 |
| 백엔드 API | http://localhost:8080 | Spring Boot API |
| Swagger UI | http://localhost:8080/swagger-ui | API 문서 |
| WebRTC 서버 | https://localhost:4443 | mediasoup 서버 |
| MySQL | localhost:3306 | 데이터베이스 |
| Redis | localhost:6379 | 캐시 서버 |

### 프로덕션 환경
| 서비스 | URL |
|--------|-----|
| 프론트엔드 | https://promisenow.store |
| 백엔드 API | https://api.promisenow.store |
| WebRTC 서버 | https://webrtc.promisenow.store |

---

## 🔧 환경 변수 설정

### 백엔드 환경 변수 (`backend-spring/.env`)

`env.example` 파일을 참고하여 다음 변수들을 설정하세요:

```bash
# 데이터베이스 설정
SPRING_DATASOURCE_URL="jdbc:mysql://localhost:3306/ssafy-mysql-db"
SPRING_DATASOURCE_USERNAME="S13P11B107"
SPRING_DATASOURCE_PASSWORD="your_database_password_here"

# JWT 설정
SPRING_JWT_TOKEN="your_jwt_secret_here"

# 카카오 OAuth2 설정
SPRING_KAKAO_CLIENT_ID="your_kakao_client_id"
SPRING_KAKAO_CLIENT_SECRET="your_kakao_client_secret"
SPRING_KAKAO_REDIRECT_URI="https://api.promisenow.store/oauth2/authorization/kakao"

# Redis 설정
SPRING_REDIS_HOST="localhost"
SPRING_REDIS_PORT=6379
SPRING_REDIS_PASSWORD=""

# Webhook 설정 (선택사항)
WEBHOOK_SLACK_URL="your_slack_webhook_url"

# 프론트엔드 리다이렉트 URI
SPRING_FRONT_REDIRECT_URI="https://promisenow.store"
```

### WebRTC 서버 환경 변수 (`backend-webrtc/.env`)

```bash
# 도메인 설정
DOMAIN="localhost"  # 개발: localhost, 프로덕션: webrtc.promisenow.store

# HTTPS 인증서 경로 (프로덕션)
HTTPS_CERT_FULLCHAIN="/etc/letsencrypt/live/webrtc.promisenow.store/fullchain.pem"
HTTPS_CERT_PRIVKEY="/etc/letsencrypt/live/webrtc.promisenow.store/privkey.pem"

# mediasoup 설정
MEDIASOUP_LISTEN_IP="0.0.0.0"
MEDIASOUP_ANNOUNCED_IP="your-server-public-ip"
MEDIASOUP_MIN_PORT="40000"
MEDIASOUP_MAX_PORT="49999"
PROTOO_LISTEN_PORT="4443"
```

---

## 📁 프로젝트 구조

```
PromiseNow/
├── 📂 backend-spring/              # Spring Boot 백엔드
│   ├── src/main/java/com/promisenow/api/
│   │   ├── domain/                 # 도메인별 모듈
│   │   │   ├── user/              # 사용자 관리
│   │   │   ├── room/              # 방 관리
│   │   │   ├── chat/              # 채팅
│   │   │   ├── availability/      # 일정 조율
│   │   │   ├── roulette/          # 룰렛
│   │   │   └── leaderboard/      # 도착 순위
│   │   ├── global/                # 전역 설정
│   │   │   ├── jwt/              # JWT 인증
│   │   │   ├── oauth/            # OAuth2 인증
│   │   │   └── security/         # 보안 설정
│   │   ├── infrastructure/        # 인프라 계층
│   │   │   ├── file/             # 파일 업로드
│   │   │   └── webhook/          # Slack 웹훅
│   │   └── config/               # 설정 클래스
│   └── build.gradle
│
├── 📂 backend-webrtc/              # WebRTC 서버 (mediasoup)
│   ├── lib/                       # mediasoup 라이브러리
│   ├── server.js                  # 서버 진입점
│   └── config.js                  # 설정 파일
│
├── 📂 frontend/                    # React + TypeScript 프론트엔드
│   ├── src/
│   │   ├── apis/                  # API 클라이언트
│   │   ├── components/           # 공통 컴포넌트
│   │   ├── features/             # 기능별 모듈
│   │   │   ├── appointment/      # 약속 관리
│   │   │   ├── calendar/         # 캘린더
│   │   │   ├── chatScreen/       # 채팅 화면
│   │   │   ├── callScreen/       # 화상 통화
│   │   │   ├── map/              # 지도
│   │   │   └── ...
│   │   ├── hooks/                 # React Hooks
│   │   ├── pages/                 # 페이지 컴포넌트
│   │   ├── stores/                # 상태 관리 (Zustand)
│   │   └── lib/                   # 유틸리티
│   └── package.json
│
├── 📂 scripts/                     # 개발 스크립트
│   ├── full-dev.sh                # 전체 개발 환경 실행
│   └── cleanup.sh                 # 환경 정리
│
├── 📂 exec/                       # 실행 가이드 문서
│   ├── 1_빌드_배포_가이드.md
│   ├── 2_외부_서비스_정보.md
│   └── 4_시연_시나리오.md
│
├── docker-compose.local.yml        # 로컬 개발용 Docker 설정
├── env.example                     # 환경 변수 예시
└── README.md
```

---

## 🛠️ 기술 스택

### 백엔드
| 기술 | 버전 | 용도 |
|------|------|------|
| Spring Boot | 3.5.3 | 웹 프레임워크 |
| Java | 21 | 프로그래밍 언어 |
| Gradle | 8.x | 빌드 도구 |
| Spring Security | 3.5.3 | 보안 및 인증 |
| Spring Data JPA | 3.5.3 | 데이터베이스 ORM |
| Spring WebSocket | 3.5.3 | 실시간 통신 |
| QueryDSL | 5.0.0 | 타입 안전한 쿼리 |
| JWT | 0.11.5 | 토큰 기반 인증 |
| MySQL | 8.0 | 메인 데이터베이스 |
| Redis | 7.2 | 캐시 및 세션 저장소 |

### 프론트엔드
| 기술 | 버전 | 용도 |
|------|------|------|
| React | 19.1.0 | UI 라이브러리 |
| TypeScript | 5.8.3 | 타입 안전성 |
| Vite | 7.0.4 | 빌드 도구 |
| Tailwind CSS | 4.1.11 | 스타일링 |
| React Query | 5.83.0 | 서버 상태 관리 |
| Zustand | 5.0.6 | 클라이언트 상태 관리 |
| mediasoup-client | 3.x | WebRTC 클라이언트 |
| SockJS | 1.6.1 | WebSocket 클라이언트 |

### WebRTC 서버
| 기술 | 버전 | 용도 |
|------|------|------|
| mediasoup | 3.x | WebRTC 미디어 서버 |
| Node.js | 20+ | 런타임 환경 |
| Protoo | 4.0.5 | 시그널링 프로토콜 |

### 인프라
| 기술 | 용도 |
|------|------|
| Docker & Docker Compose | 컨테이너화 |
| Nginx | 리버스 프록시 |
| Let's Encrypt | SSL 인증서 |

---

## 📡 주요 API 엔드포인트

### 인증
- `POST /oauth2/authorization/kakao` - 카카오 OAuth2 로그인
- `POST /api/auth/refresh` - JWT 토큰 갱신

### 사용자
- `GET /api/users/me` - 내 정보 조회
- `GET /api/users/me/basic` - 내 기본 정보 조회

### 방 관리
- `POST /api/rooms` - 방 생성
- `GET /api/rooms/{roomId}` - 방 정보 조회
- `POST /api/rooms/join` - 방 참여
- `GET /api/rooms/my` - 내 방 목록 조회

### 일정 조율
- `GET /api/availability/me` - 내 일정 조회
- `POST /api/availability` - 일정 등록/수정
- `GET /api/availability/{roomId}` - 방의 모든 일정 조회
- `GET /api/availability/{roomId}/recommend` - 추천 일정 조회

### 채팅
- `GET /api/chatting/{roomId}/messages` - 채팅 메시지 조회
- `POST /api/chatting/upload/image` - 이미지 업로드

### 룰렛
- `POST /api/roulette/create` - 룰렛 항목 생성
- `GET /api/roulette/{roomId}` - 룰렛 항목 조회
- `PUT /api/roulette/{rouletteId}` - 룰렛 항목 수정
- `DELETE /api/roulette/{rouletteId}` - 룰렛 항목 삭제

### 리더보드
- `GET /api/leaderboard/{roomId}` - 리더보드 초기 데이터 조회

> 📚 전체 API 문서는 [Swagger UI](http://localhost:8080/swagger-ui)에서 확인할 수 있습니다.

---

## 🔍 문제 해결

### 백엔드 빌드 오류

```bash
# 테스트 파일 제거
rm -rf backend-spring/src/test

# 다시 빌드
cd backend-spring
./gradlew clean build -x test
```

### 프론트엔드 의존성 문제

```bash
cd frontend
rm -rf node_modules
pnpm install
```

### Docker 서비스 문제

```bash
# 서비스 재시작
docker-compose -f docker-compose.local.yml down
docker-compose -f docker-compose.local.yml up -d

# 로그 확인
docker-compose -f docker-compose.local.yml logs -f
```

### MySQL 연결 오류

```bash
# MySQL 컨테이너 상태 확인
docker-compose -f docker-compose.local.yml ps mysql

# MySQL 로그 확인
docker-compose -f docker-compose.local.yml logs mysql

# 환경 변수 확인
echo $SPRING_DATASOURCE_PASSWORD
```

### Redis 연결 오류

```bash
# Redis 컨테이너 상태 확인
docker-compose -f docker-compose.local.yml ps redis

# Redis 연결 테스트
docker-compose -f docker-compose.local.yml exec redis redis-cli ping
```

---

## 📚 문서

- 📖 [빌드 및 배포 가이드](exec/1_빌드_배포_가이드.md) - 프로덕션 배포 방법
- 🔌 [외부 서비스 정보](exec/2_외부_서비스_정보.md) - 카카오 OAuth, mediasoup 등 설정
- 🎬 [시연 시나리오](exec/4_시연_시나리오.md) - 서비스 시연 가이드

---

## 🔒 보안 주의사항

⚠️ **중요**: 민감한 정보는 절대 저장소에 커밋하지 마세요!

- ✅ `.env` 파일은 `.gitignore`에 포함되어 있습니다
- ✅ 실제 비밀번호, API 키, 토큰은 환경 변수로 관리하세요
- ✅ `env.example` 파일을 참고하여 필요한 환경 변수를 설정하세요
- ✅ 프로덕션 환경에서는 강력한 비밀번호를 사용하세요

---

## 📞 연락처

- **프로젝트**: 삼성 청년 SWㆍAI 아카데미 프로젝트

---

## 📝 라이선스

이 프로젝트는 삼성 청년 SWㆍAI 아카데미 프로젝트입니다.

---

<div align="center">

**Made with ❤️ by 삼성 청년 SWㆍAI 아카데미 13기 공통프로젝트 대전 1반 B107 Team.일단해**

[⬆ Back to Top](#-promisenow)

</div>
