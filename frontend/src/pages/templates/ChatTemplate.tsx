import RoomLayout from '../../components/layout/RoomLayout';
import ChatScreen from '../../features/chatScreen/components/ChatScreen';

const ChatTemplate = () => {
  return (
    <RoomLayout>
      <ChatScreen />
    </RoomLayout>
  );
};

export default ChatTemplate;
